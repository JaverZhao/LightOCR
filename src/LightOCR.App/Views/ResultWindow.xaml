<Window x:Class="LightOCR.App.Views.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR 结果" Height="500" Width="700"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding ElapsedText}"
                       VerticalAlignment="Center" Foreground="Gray" />
        </Grid>

        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="4">
            <TextBox Text="{Binding EditableText, Mode=TwoWay}"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="复制全文" Command="{Binding CopyAllCommand}"
                    Padding="12,4" Margin="4,0" />
            <Button Content="关闭" Command="{Binding CloseCommand}"
                    Padding="12,4" Margin="4,0" />
        </StackPanel>
    </Grid>
</Window>
